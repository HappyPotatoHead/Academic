---
aliases:
  - May 2023
tags:
  - PYQ
  - Programming
Creation Date: 2023-10-02T22:10:00
Completion: true
obsidianUIMode: preview
---
# Section A
## Question 1
### Question a
#### (ii)
```cpp
cout << a + c << endl; --> 6
cout << e - d << endl; --> 6.38
cout << a * d << endl; --> 2.5
cout << fixed << setprecision(2) << e/b << endl; --> 0.88
```
#### (ii)
```cpp
#include <iostream>
#include <cctype>
using namespace std;

int main(){
	cout << "Display The Output As Below: \n";
	cout << "1. " << second << endl;
	cout << "2. " << b*10 << "% " << second << endl;
	cout << "3. " << first << " " << second << " " << third << endl;
	cout << toupper(first) << toupper(third) << endl;	
return 0;
}
```
### Question b
```cpp
#include <iostream>
using namespace std;

int main(){
	// allows user to input two integers as numerator and denominator. 
	// Determine whether there is a remainder or not when the numerator is divided by the denominator
	// Find remainder
	// input --> declare variables
	int numerator, denominator;
	cout << "Please key in the numerator: ";
	cin >> numerator;
	cout << "Please key in the denominator: ";
	cin >> denominator;

	if (numerator == 0)
		cout << "The fraction is zero! \n";
	else if (denominator ==0)
		cout << "The fraction is undefined! \n";
	else if (numerator % denominator == 0)
		cout << numerator << " / " << denominator << " has no remainder. \n";
	else
		cout << "The remainder of " << numerator << " / " << denominator << numerator % denominator << "." <<< endl;
	return 0;
}
```
## Question 2
### Question a
```cpp
int x = 43;
int (x * 3.4 / 2) -> bracket initialisation
--> 43 * 1.7
--> 73

12 + x * 5 / float (4)
--> 12 + 43 * 5 / 4.0
--> 12 + 43 * 1.25
--> 65.75

(x - 6) % 10 - 5.0
--> 37 % 10 - 5.0 
--> 7 - 5.0
--> 2.0

static_cast<int>(x*3.3/6)
--> static_cast<int>(43*0.55)
--> 23
```
### Question b
```cpp
int z = 33, y = 5;
z %= --y;
--> z = z % (4)
--> z = 33 % 4
--> z = 1
if(z-- || (y-4 && 0))
1 || (1 && 0)
1 || 0
true

if(!(++y < 4 || !(2 <= z++)))
!(6 < 4 || !(2 <= 1))
!(false || !(false))
!(false || true)
!(true)
false
```
### Question c
```cpp
#include <iostream>
#include <iomanip>
#include <cctype>

using namespace std;

int main(){
	// allows user to select their choice of Nintendo games from the list
	// re-enter if their input is not valid. 
	// 10% discount if the purchae > 500
	int choice;
	char cont;
	double total_price=0;
	
	cout << "Nintendo Games List:" << endl;
	cout << "1. Animal Crossing: New Horizons" << endl;
	cout << "2. The Legend of Zelda: Breath of the Wild" << endl;
	cout << "3. Pokemon Scarlet" << endl;
	cout << "4. Super Mario 3D All Star" << endl;
	do{
		cout << "Pick a choice from the list: ";
		cin >> choice;
		if(choice == 1)
			total_price += 185.90;
		else if(choice == 2)
			total_price += 188.90;
		else if(choice == 3)
			total_price += 198.90;
		else if(choice == 4)
			total_price += 182.90;
		else
			cout << "Invalid chocie! Please try again later." << endl;
		cout << "Do you want to continue shopping? " << endl;
		cin >> cont;
	}while(toupper(cont) == 'Y');
	cout << "Please pay RM " << total_price << endl;
	return 0;
}
```
## Question 3
```cpp
#include <iostream>
#include <iomanip>
#include <fstream>

using namespace std;

#define no 6

void in90s(string manga[], int year[]);
void bestSelling(string manga[], int yearp[], double totalSales[]);
void saveManga(string manga[], int year[], double totalSales[]);

int main(){
	string manga[no]={"Slam Dunk", "Naruto", "Doraemon", "One Piece", "Demon Slayer: Kimetsu No Yaiba", "Dragon Ball"};
	int year[no] = {1990, 1999, 1969, 1997, 2016, 1984};
	double totalSales[no] = {170, 250, 250, 516.6, 150, 300};
	
	void in90s(manga, year);
	void bestSelling(manga, year, totalSales);
	saveManga(manga, year, totalSales);
	
	return 0;
}

void in90s(string manga[], int year[]){
// display the manga realeased in the 90s
// 90s range -> 1990 -> 2000 = 10 years gap
	int count = 1;
	cout << "Manga Released in 90s:\n";
	for(int i = 0; i < no; i++){
		if(2000 - year[i] <= 10)
			cout << ++count << ". "<< manga[i] << endl; 
	}
}

void bestSelling(string manga[], int year[], double totalSales[]){
	double max = totalSales[0];
	int index;
	for(int i = 0; i < no; i++){
		if (totalSales[i] > max){
			max = totalSales[i];
			index = i
		}
	}
	cout << "Best Selling Manga" << endl;
	cout << "Name: " << manga[i] << endl;
	cout << "Year of Released: " << year[i] << endl;
	cout << "Total Sales: " << max * 1e6 << endl;
}

void saveManga(string manga[], int year[], double totalSales[]){
// List them in alphabetical order
// First way -> string way
// use str.compare(str) --> if it is 0 = remain
// if it is 1 = str1 comes first
// if it is -1 = str2 comes first
// Second way -> character way
// Turn them into c-string but that's not possible
// scrap this idea
// save into text file
	ofstream writeFile("manga.txt");
	string temp_name;
	int temp_year;
	double temp_sales
	if (writeFile.fail())
		cout << "File has failed to open" << endl;
	else{
		writeFile << "Manga" << right << setw(20) << "Year of Released" << right << setw(20) << "Total Sales(M)";
		for(int i = 0; i < no; i++ ){
			for(int k = 0; k < no - i; k++){
				if(manga[k].compare(manga[k+1]) == 1){
					temp_name = manga[k];
					manga[k] = manga[k+1];
					manga[k+1] = temp_name;
					
					temp_year = year[k];
					manga[k] = manga[k+1];
					manga[k+1] = temp_year;
					
					temp_sales = totalSales[k];
					toalSales[k] = totalSales[k+1];
					totalSales[k+1] = temp_sales;
					}
				}
			}
		for(int i = 0; i < no; i++){
				writeFile << manga[i] << right << setw(30) << year[i] << right << setw(30) << right << totalSales[i] << endl;
			}
		}
	writeFile.close();
}
```
___
# Section B
## Question 4
### Question a
```cpp
struct Cloth{
	string code;
	char size[3];
	float price;
	int stock;
};
```
### Question b
```cpp
Cloth clothes[100];
```
### Question c
```cpp
void writeClothesDetails(Cloth clothes[], int &noCloth){
	// stores the clothing information into the struct array declared
	// user is required to enter the values for each variable in the struct Cloth repeatedly -> do while loop
	// noCloth is used to keep track of the numebr of clothes stores in the array clothes -> noCloth is initially 0
	// write the information of clothes into a text file named clothDetails.txt
	char cont;
	do{
		cout << "Code of Cloth: ";
		cin >> clothes[noCloth].code;
		cout << "Size: ";
		cin >> clothes[noCloth].size;
		cout << "Price: ";
		cin >> clothes[noCloth].price;
		cout << "Number of stocks: ";
		cin >> clothes[noCloth].stock;
		cout << "Do you want to continue?(Y - Yes, N - No): ";
		cin >> cont;
		if (toupper(cont) == ' Y')
			noCloth++;
	}while(toupper(cont) == 'Y');
	ofstream writeFile("clothDetails.txt");
	if (writeFile.fail())
		cout << "File has failed to open";
	else{
		for(int i =0; i < noCloth; i++){
			writeFile << clothes[i].code << endl;
			writeFile << clothes[i].size << endl;
			writeFile << clothes[i].price << endl;
			writeFile << clothes[i].stock << endl;
		}
	}
}
```
### Question d
```cpp
void findClothesPrices(Cloth clothes[], int noCloth){
	// input the details of the clothes he/she wants to purchase
	// display the code, size and the total price of the selected cloth according to the quantity
	// the code and quantity of the clothes to be purchased depends on the availabitlity of the stock
	string code;
	char size[3];
	int number;
	double total_price = 0; 
	cout << "Please enter the code of the cltohes you wish to purchase: ";
	cin >> code;
	cout << "Pleaes enter the size of " << code << ": ";
	cin >> size;
	cout << "Please enter the number of clothes that you want to purchase: ";
	cin >> number;
	// Assuming the codes entere are correct
	for(int i =0; i < noCloth;i++){
		if(clothes[i].code == code){
			cout << "Cloth: " << clothes[i].code;
			cout << "Size: " << clothes[i].size;
			if(clothes[i].stock <= number){
				total_price += clothes[i].price * number;
				cout << "Total price of " << number << " is RM " << total_price;
			}
			else
				cout << "Not Available!";
		}
	}
}
```
## Question 5
### Question a
```cpp
struct MarketShare{
	string brand;
	float percentage[2];
	double rate;
};
```
### Question b
```cpp
MarketShare share[4];
```
### Question c
```cpp
void recordMarkertShare(MarketShare share[], int count){
	// prompts the user to input the brand of smartphone as indicated
	// count is used to keep track of the number of share information
	cout << "Pleae key in the details accordingly" << endl;
	for(int i = 0; i < count; i++){
		cout << "Brand of smartphone: ";
		cin >> share[i].brand;
		cout << "Share market in Jan 2022: ";
		cin >> share[i].percentage[0];
		cout << "Share market in Jan 2023: ";
		cin >> share[i].percentage[1];
	}
}
```
### Question d
```cpp
void calculateChangeRate(MarketShare share[], int count){
	// calculates the annual percentage rate based on the brand of smartphone
	// determine the brand of smartphone with the highest increment among all
	for(int i =0; i < count; i++){
		share[i].rate = share[i].percentage[1] - share[i].percentage[0];
		cout << share[i].brand << " --> " << fixed << setprecision(1) << share[i].rate << "%"<<endl;
	}
	double max = share[0].rate;
	int position;
	for(int k =0; k < count; k++){
		if(share[k].rate < share[k+1].rate){
			max = share[k+!].rate;
			position = k;
		}
	}
	cout << "In Jan 2023, " << share[position].brand << " recorded the highest increment of " << share[position].rate << "% to " << share[position].percentage[0] << "% as compared to " << share[position].percentage[1] << "% in Jan 2022";
}
```
### Question c
```cpp
void printRecord(MarketShare share[], int count){
	ofstream writeFile("RecordMarkertShare.txt")
	if (writeFile.fail())
		cout << "File has failed to open" << endl;
	else{
		writeFile << "Brand of Smartphone     Jan 2022(%)    Jan 2023(%)  Change Rate(%)" << endl;
		for(int i = 0; i < count; i++){
			writeFile << share[i].brand << share[i].percentage[0] << share[i].percentage[1] << share[i].rate << endl;
		}
	}
}
```
___
# Next Paper
[[Past Year Papers/Year 2/Semester 1/Front-End Web Development/May 2024]]